ENABLE_TESTING()

FILE(GLOB_RECURSE TEST_FILES ${PROJECT_SOURCE_DIR}/test/*.cpp)
ADD_EXECUTABLE(
        KZG_TEST
        ${TEST_FILES}
)

TARGET_LINK_LIBRARIES(
        KZG_TEST
        GTest::gtest_main
        KZG_Commitment
)

INCLUDE(GoogleTest)
GTEST_DISCOVER_TESTS(KZG_TEST)